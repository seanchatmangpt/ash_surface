# chicago-doctest-sweep-052: doctests for every boundary public
status: DONE
created: 2026-09-17T06:30:00Z
## Mission
First-doctests landed for from_manifest/compile. Extend: project_ir/2 (one projector), transport select/3, Standing validators, MXEpisode.compose/1, Intent.create/3 — each with runnable iex examples asserting real output; modules lacking any public doc noted. Subject: lib docstrings + test/ash_surface/doctest_test.exs.
## Definition of Done (Chicago school)
- State-based tests exercise the REAL subject — no test doubles for the unit under test (injected seams only where the law itself demands injection).
- Assertions on observable outcomes only: returned values, emitted artifacts, on-disk bytes, typed refusals — never internals (mock-call bookkeeping allowed solely where the law IS the boundary, e.g. bus-untouched proofs).
- EXECUTED falsifier in the ticket History: mutate the subject behavior, run the new tests, show RED, restore, show GREEN — commands + exits recorded.
- Gates exit 0: mix compile --warnings-as-errors; mix test; npm test (if JS touched); mix format --check-formatted.
## Acceptance
- >=6 new doctest groups green; falsifier: wrong expected output in one example -> RED; gates 0.
## History
2026-09-17T06:15:46Z | ALIVE | exp/chicago-doctest-sweep-52 @ edf525b (+549e0b4 gate repair) | mix deps.get&&npm install exit 0; mix compile --warnings-as-errors exit 0; mix test exit 0 — 871 passed (34 doctests, 837 tests; baseline was 5 doctests, +29 new); mix format --check-formatted exit 0 (after formatter pass on 2 PRE-EXISTING drifted test files mx_episode_compose_test.exs + projector_ir_determinism_test.exs — drift on HEAD predates this ticket, commit 549e0b4, zero semantic change); npm test NOT run — no JS touched | 6 new doctest groups green: Transport.select/3 (first @doc: undelegated preference law, dimension-weighed frontier — preference never picks a dominated alternative, typed refusals incl. corrected pin that :unknown_transport membership gate fires before :unadmitted_transport), Transport.facts_from_profile/1, Standing.refused?/1 (valid?/validate! examples already on lib, now wired runnable via `doctest AshSurface.Standing`), MXEpisode.compose/1 (real Observation/PlanningEpisode/Event constructors, 5-step witnesses, subject-binding + missing-parts refusals), Intent.create/3 (doctest registers create/4 — def carries default opts; pinned sha256 id 79f3ebbf...b5a2, time-is-not-identity replay), Projectors.ARIA.project_ir/2 (contract shape, facts-read-never-infer, OBSERVE-only live hint) — all state-based on the real subjects, zero doubles for the units under test | FALSIFIER EXECUTED: mutate lib/ash_surface/projectors/aria.ex politeness(nil) "polite"->"assertive"; mix test test/ash_surface/doctest_test.exs -> exit 2, 33/34 passed, exactly "doctest AshSurface.Projectors.ARIA.project_ir/2 (2)" RED (left "assertive" right "polite"); restore -> exit 0, 34 passed GREEN | modules lacking public doc: every lib module carries @moduledoc (verified); Transport.mark_dispatched/1 + fallback_allowed?/1 are public with bare @spec, no @doc — noted, not in this ticket's extend list; formatter behaviour callbacks (AshSurface.Formatter extensions/features/format) bare by convention | operator wrote nothing — all changed bytes manufactured in-session; no HANDWRITTEN.md/UNSUPPORTED deltas (docs+examples on admitted files, no new projection debt) | remaining: full-lib per-function @doc audit open; falsifier-only observation: falsifier loop's first RED run exit code was mis-recorded (zsh PIPESTATUS), true exit re-executed and recorded as 2.
